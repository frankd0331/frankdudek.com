# frankdudek.com

## Usage:
Ok, looks like you run `jekyll build` then you add, commit, and push... seems to work
but I was kinda expecting Jekyll to handle everything, but it's not soley built
for github

## TODO:
- [ ] post.rb still seems funky, it opens a blank md file not the one with the
header
